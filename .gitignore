# Hone review state (local by default — remove this line to commit reports)
.hone/

# Dev notes (local)
CLAUDE.md

# Test sandbox (real specs, not committed)
tests/sandbox/*
!tests/sandbox/.gitkeep

# OS
.DS_Store

# Editor
*.swp
*.swo
*~

# Node (if we add tooling later)
node_modules/
